import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="Home"
      note="Dashboard: alerts, to-bake-today, today's orders, stats strip. Built in Phase 10."
    />
  );
}
